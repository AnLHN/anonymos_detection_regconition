'use client';

import type { Alert, Camera, Employee, Rule } from '@/lib/types';

export default function DashboardHome(_props: { alerts: Alert[]; cameras: Camera[]; rules: Rule[]; employees: Employee[] }) {
  return null;
}
